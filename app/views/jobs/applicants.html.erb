<h1> Applicants for <%= @job.position %> </h1> 
<% if @job.job_applications.size >= 1 %>
    <% @job.job_applications.each do |application| %>
      <%=link_to application.user.name, resume_document_path(application.user.id), :method=>:get %>
      <br>
      ---------------------------------------------
      <br>
    <% end %>
<% end %>
