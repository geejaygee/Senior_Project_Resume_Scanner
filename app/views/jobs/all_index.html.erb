<h1> All Jobs </h1>
<% if @jobs.size < 1 %>
Sorry, currently no jobs posted 
<% else %>
  <% @employers.each do |employer| %>
    <% employer.jobs.each do |job| %>
      <%= link_to job.position, job, :method=>:get %>
      <br>
      <%=employer.companyName %>
      <br>
      <br>
---------------------------------------------------
    <br>
    <% end %>
    <br>
  <% end %>
<% end %>
<%= button_to 'Home', root_path, :class=>"button", :method => :get %>
